import { printf } from "<libc>"
import { foreach } from "utils/uts-array"

const main = (argc, argv) => {
	printf("argc = %i\n", argc)

	foreach(
		argv, argc, 
		(item, index) => {
			printf("argv[%i] = %s\n", index, item)
		}
	)

	return 0
}

export { main }