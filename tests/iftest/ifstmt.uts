import { puts } from "<libc>"

let main = () => {
	const x = 1
	const y = 1

	if (x)
	{
		if (y)
		{
			puts("x and y were both truthy!")
		}
		else
		{
			puts("x is truthy!")
		}
		
	}
	else if (y)
	{
		puts("x was false :( but y was truthy!")
	}
	else
	{
		puts("everything was false :(")
	}

	return 0
}

export { main }